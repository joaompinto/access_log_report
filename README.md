# Generate reports from access logs files

Main Features
-------------
- Support for custom log formats
- Generates throughput (rps) / performance (avg_msec) charts

Usage:
------
    acl2csv.py sample.cfg access_log > sample.csv
    acl2png.py sample.cfg sample.csv
    ls -ltr *.png
